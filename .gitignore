# Data tree — large and regenerable via the pipeline (clips, corpora, embedding
# caches, models, external reference datasets). Only code + docs are tracked.
data/

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
*.egg-info/
build/
dist/
.pytest_cache/
.ruff_cache/

# Node / Playwright
node_modules/
test-results/
playwright-report/

# OS / editor
.DS_Store
*.swp

# generated artifacts
.coverage
docs/scorecard.json

# Собранный сервисом корпус: пользовательское аудио и персональные данные.
# Хранится вне репозитория (152-ФЗ), в проде переезжает в S3 и PostgreSQL.
corpus/
